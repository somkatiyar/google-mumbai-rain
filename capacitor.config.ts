import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.skymet.mumbairain',
  appName: 'mumbairain',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
